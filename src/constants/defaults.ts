export const DEFAULT_CITY_NAMES = ['ЦГБ-П', 'ОКБ-М', 'ЦГБ-Н'] as const;

export const DEFAULT_ORGANIZATION = 'МЗ';

export const INPUT_CLASSES = {
    base: "form-input",
    listItem: "list-input",
    textarea: "form-textarea",
    textareaActive: "form-textarea form-textarea-active",
    textareaParsed: "form-textarea form-textarea-parsed"
} as const;

export const BUTTON_CLASSES = {
    primary: "btn-primary",
    secondary: "btn-secondary",
    add: "btn-add",
    addFull: "btn-add-full"
} as const;